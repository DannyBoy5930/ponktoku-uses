---
import MainLayout from "@/layouts/Layout.astro";

interface Props {
	title: string;
}

const { title = "What I Use - Danny Bao" } = Astro.props;

const { Content } = await post.render();
---

<MainLayout title="What I Use">
  <article>
    <Content></Content>
  </article>
</MainLayout>
